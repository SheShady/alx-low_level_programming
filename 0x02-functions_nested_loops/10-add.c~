#include "main.h"
/**
 * add - adds two integers from input
 * @a: first integer
 * @b: second integer
 *
 * Description: adds two numbers
 * Return: Always (0).
 */
int add(int a, int b)  
{
return (a + b);
}
