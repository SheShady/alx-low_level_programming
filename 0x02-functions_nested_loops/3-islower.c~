#include "main.h"
/**
 * _islower - Entry point
 * Description - function checks for lower case characters
 *
 * @c: int value it receives
 * Return: SUCCESS
 */
int _islower(int c)
{
int i = 'a';
for (i = 'a'; i = 'z'; i++)
{
if c == i
{
return (1);
}
}
return (0);
}
